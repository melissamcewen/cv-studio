
extends: existence
message: "Bullet point overflows the first line and isn't long enough to fill a second"
level: warning
scope: raw
raw:
   - '(?m)^\s*-\s("||)(?!.*\]\([^)]*\)).{85,95}(?: \S+){0,4}$'
